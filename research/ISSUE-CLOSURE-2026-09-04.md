# Issue closure record — 2026-09-04

| Issue | Title | Resolved by | Merged |
|---|---|---|---|
| #1776 | perf(npu): Zaya decode — attention weights streamed per token | #2091 single-launch fused GU→SiLU→D | 2026-09-04 |
| #2084 | ci: census coverage gate deadlocks same-day census PRs | #2092 coverage gate advisory on PRs | 2026-09-04 |

Both PRs landed via the merge queue, but the issues did not auto-close (the
squash-commit titles carry plain `(#NNNN)` references, not `Closes`/`Fixes`
keywords). This follow-up records the closure explicitly.
