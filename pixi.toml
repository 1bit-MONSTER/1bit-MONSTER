# The Mojo fold (docs/plans/mojo-fold-1bit-monster.md): all non-kernel tooling
# is Mojo 1.0. Pin discipline: mojo==1.0.0, no beta pins anywhere.
[project]
name = "1bit"
description = "1-bit LLM inference engine — C++23 kernels, Mojo 1.0 tooling"
channels = ["https://conda.modular.com/max", "conda-forge"]
platforms = ["linux-64"]

[dependencies]
mojo = "==1.0.0"
